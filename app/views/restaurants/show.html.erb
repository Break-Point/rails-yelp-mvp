  <h1> <%= @restaurant.name %> </h1>

  <h2> <%= @restaurant.category %> </h2>

  <h3> <%= @restaurant.address %> </h3>

  <h4> <%= @restaurant.phone_number %> </h4>

  <h2>Reviews ⭐</h2>
  <ul>
    <% @restaurant.reviews.each do |review| %>
      <li>
        <%= review.content %>
        <%= "⭐" * review.rating %>
      </li>
    <% end %>
  </ul>

  <%# <%= link_to "Add a review", new_restaurant_review_path(@restaurant), class: "btn btn-sm btn-outline-secondary m-1" %> 

  <%= render 'reviews/new' %>
  